export default function ProductList() {
    return (
        <div id="productlist">
            Hello from ProductList

            <p>
                ProductList: Displays available 
                products to add to the cart.
            </p>

        </div>
        );
}